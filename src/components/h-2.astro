---
export interface Props {
  text: string;
  classes: string;
}
const { text, classes } = Astro.props;
---

<h2
  class={`text-transparent bg-clip-text bg-gradient-to-r from-sky-400 to-cyan-300 text-3xl font-semibold ${classes}`}
>
  {text}
</h2>
