---
import Layout from './general.astro';

export interface Props {
  meta: {
    title: string;
    description: string;
    image: string;
    url: string;
  };
}

const { meta } = Astro.props;
---

<Layout meta={meta}>
  <slot />
</Layout>
